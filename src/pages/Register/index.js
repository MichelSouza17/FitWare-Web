import Header from "../../components/Header";
import { ContainerAll } from "./styles";

function Register() {
    return (
        <ContainerAll>
            <Header />
            
        </ContainerAll>
    );
}

export default Register;
